# Phase 1 — ML (정형 데이터 분석/분류)
pandas
numpy
scikit-learn==1.7.2   # .pkl 모델 저장 버전 고정 (배포 시 로딩 호환)
xgboost
matplotlib
seaborn
ipykernel

# Streamlit 데모 (멀티페이지 st.navigation 사용 → 1.36+ 필수)
streamlit>=1.40

# 노트북 빌드·실행 (src/ml/build_notebook.py)
nbformat
jupyter

# --- Phase 2 — DL (Apple Silicon: MPS / OCI ARM(aarch64): CPU 휠 자동) ---
# aarch64에선 `pip install torch`가 CPU 빌드를 자동 설치 → 별도 index 불필요
torch>=2.2
torchvision>=0.17
ultralytics>=8.3        # YOLOv8 병해 잎 위치 검출 (src/dl/05_detect.py · 청크 2-11)

# --- Phase 3 — LLM (새 세션에서 주석 해제) ---
# anthropic
# python-dotenv
mlflow>=2.9
