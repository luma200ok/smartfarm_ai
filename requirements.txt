# Phase 1 — ML (정형 데이터 분석/분류)
pandas
numpy
scikit-learn==1.7.2   # .pkl 모델 저장 버전 고정 (배포 시 로딩 호환)
xgboost
matplotlib
seaborn
ipykernel

# Streamlit 데모 (멀티페이지 st.navigation 사용 → 1.36+ 필수)
streamlit>=1.40

# 노트북 빌드·실행 (src/ml/build_notebook.py)
nbformat
jupyter

# --- Phase 2 — DL (Apple Silicon: MPS / OCI ARM(aarch64): CPU 휠 자동) ---
# aarch64에선 `pip install torch`가 CPU 빌드를 자동 설치 → 별도 index 불필요
torch>=2.2
torchvision>=0.17
ultralytics>=8.3        # YOLOv8 병해 잎 위치 검출 (src/dl/05_detect.py · 청크 2-11)

# --- Phase 3 — LLM (Ollama 로컬 구동: qwen2.5:14b) ---
ollama              # Ollama Python 클라이언트 (로컬 데몬 http://localhost:11434 호출)
python-dotenv       # 디스코드 웹훅 URL 등 시크릿 로딩(.env)
requests            # 디스코드 Webhook 알림 발송 (Phase 3-4)
pytest              # Phase 3-1 테스트 (tests/) — 진단·게이트·처방 로직 검증
mlflow>=2.9

# PostgreSQL + pgvector — RAG 저장소·처방/경보 이력 (선택, DATABASE_URL 미설정 시 미사용)
psycopg[binary]      # psycopg3 — connect-per-call, 커넥션풀 미사용(연결 few ms)
pgvector             # vector 타입 어댑터(register_vector) — aarch64 휠 제공
