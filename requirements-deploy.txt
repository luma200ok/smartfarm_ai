# 배포(서버) 전용 런타임 의존성 — app/ 가 실제 import 하는 것만.
# dev/노트북/EDA(jupyter·ydata-profiling·phik·matplotlib 등)는 제외 → aarch64에서 소스 빌드 불필요.
# 업데이트: app/ import 가 바뀌면 이 파일도 동기화. (전체 개발용은 requirements.txt)

# Phase 1 — ML
pandas
numpy
scikit-learn==1.7.2      # .pkl payload 로딩 호환 (LabelEncoder 등)
xgboost                  # Phase1 분류 모델(.pkl) 본체
joblib                   # .pkl 로드

# Streamlit 멀티페이지 (st.navigation → 1.40+)
streamlit>=1.40
altair>=5.0,<6           # app/views/monitor.py 오늘 운영 추이 차트(이슈 #25) — streamlit 전이 의존성에 우연히 의존 중이던 것을 명시
requests>=2.31           # app/api_client.py 서빙 API 클라이언트(이슈 #59 PR 3) — streamlit 전이 의존이지만 직접 import라 명시

# FastAPI 서빙 API (이슈 #59 PR 4, api/) — requirements.txt와 버전 일치. httpx(TestClient)는 테스트 전용이라 제외
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # UploadFile(멀티파트 이미지·폼) 파싱에 FastAPI가 요구

# Phase 2 — DL (aarch64: torch/torchvision CPU 휠 자동)
torch>=2.2
torchvision>=0.17
ultralytics>=8.3         # YOLOv8 검출 (phase2_dl.load_yolo)
Pillow                   # PIL — 이미지 업로드/전처리
matplotlib               # phase2_dl Grad-CAM 컬러맵(직접 import) — ultralytics 경유 말고 명시

# Phase 3+ — PostgreSQL + pgvector (RAG 저장소·처방/경보 이력, DATABASE_URL 미설정 시 미사용)
psycopg[binary]
pgvector

# Phase 3 — LLM (이슈 #68 도커화 검증 중 발견: 기존에 이 파일에서 누락돼 있었음 —
# src/llm/prescribe.py·pipeline.py·rag/store.py·app/views/prescribe.py가 모듈 최상단에서
# `import ollama` 하므로 api/routers/prescriptions.py 임포트 체인 전체가 이 패키지 없이는
# 기동조차 불가. python-dotenv도 app/api_client.py·src/llm/{db,weather,notify,prescribe}.py가
# 최상단에서 `from dotenv import load_dotenv` — 동일하게 필수. requirements.txt와 이름 일치.
ollama
python-dotenv
