# Python venv
.venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# 데이터 (용량 큼 · 원본은 Kaggle/포털에서 재다운 가능)
data/*.csv
data/**/*.csv
data/**/*.hwpx
data/**/*.xlsx
data/*.xlsx
data/*.zip
data/images/
!data/.gitkeep

# docs (로컬 전용 — 커밋 제외)
docs/_local/

# 모델 저장 파일 (Phase 2~)
*.h5
*.pt
*.pkl
*.joblib
# 단, Phase 1 데모 모델은 배포 앱이 런타임에 불러야 해서 예외(슬라이더 범위·작물 통계 동봉)
!models/phase1_crop_env_clf.pkl

# OS
.DS_Store

# 환경변수 (Phase 3 API 키)
.env

# 브라우저 자동화(Playwright MCP) 잔여물
.playwright-mcp/

# Phase2 DL 산출물(대용량/캐시)
data/torchvision/
data/tomato/
data/tomato_yolo/
data/tomato_part/
data/tomato_v1_mixed/
data/plantvillage_tomato/
data/nongsaro/.rag_index.npz
runs/
yolov8n.pt

# 홈화면 캡처(로컬 보관 · 외부 업로드용, 레포 추적 제외)
docs/figures/phase2_dl/home.png

# MLflow 실험 추적 로컬 스토어(대용량 · 아티팩트 포함)
mlruns/
mlflow.db
mlartifacts/

# Phase2 발표 PDF(AI Hub 원본 사진 포함 가능 · 로컬 보관)
docs/*.pdf
